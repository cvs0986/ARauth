apiVersion: v1
kind: Service
metadata:
  name: {{ include "nuage-identity.fullname" . }}
  labels:
    {{- include "nuage-identity.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "nuage-identity.selectorLabels" . | nindent 4 }}

